@model TaskCategory

@if (Model is not null)
{
    <div class="container d-flex flex-column">
        <h3>@Model.Name</h3>
        <p>@Model.CreatedAt.ToString("dd/MM/yyyy HH:mm")</p>
    </div>
}
else
{
    <h3 class="alert alert-warning">No Category Found</h3>
}
